class student
{
    static get collegeName()
        {
            var clgname="Luminar";
            return clgname;

        }
        sestudent(rol,name)
        {
            this.rol=rol;
            this.name=name;
        }
        getstudent()
        {
            console.log("name"+name);
        }
    )
}